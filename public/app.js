document.addEventListener("DOMContentLoaded", () => {
  // Initialisiere Funktionen hier, nachdem das DOM geladen ist
  initializeCalendar();
  setupSearchAndFilter();
  setupModal();
  setupAddExercise();
});
